# Progress

| 優先 | # | 概要 | 状態 | 影響度 | 重要度 | 難易度 | 工数 |
|---|---|---|---|---|---|---|---|
| 1 | T1 | 本番の IdP へ登録し、停止の配送と定期照合を実測する | ⬜未着手 | 大 | 大 | 小 | 小 |

詳細:

1. T1: ADR-0003 の実装は入ったが、⚠ **本番では何も有効になっていない。** 効かせるには
   IdP 側の 3 手（停止の通知の送り先を登録・サービスアカウントを登録・アプリへ結び付け）と、
   `MACHINE_CLIENT_ID` などの配布が要る（手順は `docs/OPERATIONS.md`）。
   ⚠ **入れたことと効いていることは別**なので、次の 2 つをログで確かめるまで完了にしない。
   - 停止の通知が届く（`auth.oidc.logout_received`）
   - 定期照合が回る（`auth.oidc.reconciliation.finished`。⚠ `not_bound` のままなら
     アプリへの結び付けが済んでいない）
